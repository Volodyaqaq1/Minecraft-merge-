# Quick Start: Next Task for Incoming AI Agent

## 1. Текущая стадия разработки
- **Стадия**: **Stage 2 — Visual UI Polish & Redesign** (приведение визуального стиля игры к качеству топ-хитов Яндекс Игр уровня «Сквиши Мерж: Дамплинг»).
- **Архитектурный фундамент (Stage 1.1)**: **100% готов и протестирован**. WebGL backing buffer рендерится в 1920x1080 (HD), логические координаты игры строго зафиксированы на **960x540**, апскейлинг устранен (`upscaleRatio = 0.912x`).

---

## 2. Что только что закончено
1. **Фундамент казуальных 3D UI компонентов** в `src/utils/helpers.js`:
   - `createCasualButton`: тактильная кнопка с 3D фаской, бликом, клик-прессом и звуком.
   - `drawCasualCard`: мягкая карточка с деликатной тенью и скруглением.
   - `drawCasualProgressBar`: желобок прогресс-бара с глянцевым бликом.
   - `darkenColor` и `lightenColor` для автоматической генерации теней.
2. **Top Bar в `src/scenes/GameScene.js`**:
   - Тактильная 3D-кнопка настроек.
   - Тактильный 3D-бейдж «Уровень X» со звездой.
   - Казуальная 3D-кнопка «Подарки 🎁» с пульсирующим красным бейджем уведомления.
   - Неоновая шкала комбо-множителя в темном желобе с цветным заполнением и глянцем.
   - Белая глянцевая карточка баланса изумрудов.

---

## 3. Следующая задача (NEXT TASK)
**Завершить Stage 2 Visual UI Polish для оставшихся экранов:**

1. **Панель квестов (`_renderQuests` в `GameScene.js`)**:
   - Превратить карточки заданий в стикеры с мягкой тенью и булавкой.
   - Добавить круглую подложку (блюдце) под портрет моба.
   - Заменить текстовый счетчик на аккуратный мини-прогресс-бар `drawCasualProgressBar` + `2/4`.
   - Кнопку `ЗАБРАТЬ 🎁` оформить через `createCasualButton` (зеленая, 3D, с легкой пульсацией внимания).
2. **Магазин справа (`_updateShopButton` в `GameScene.js`)**:
   - Слот 1 (монеты): мягкая голубая пастельная карточка, круглый 3D постамент под моба, парящий моб с легким hover-твином, кнопка стоимости `createCasualButton` (янтарно-золотая, 3D, `💎 X`).
   - Слот 2 (реклама): мягкая сиреневая карточка, парящий моб, кнопка `createCasualButton` (фиолетовая, 3D, `🎬 Реклама`).
3. **Нижний док (`_buildBottomBar` в `GameScene.js`)**:
   - Полупрозрачная стеклянная подложка (`0x0f172a, alpha 0.65`, тонкая рамка `0x334155`).
   - 4 тактильные 3D кнопки:
     - `[📖 Бестиарий]` (синяя)
     - `[🥚 Инкубатор]` (фиолетовая)
     - `[🎁 Награды]` (бирюзовая)
     - `[⚔️ В БОЙ!]` (большая сочная рубиново-красная 3D-кнопка с `pulse: true`).
4. **Арена боя (`BattleScene.js`)**:
   - **Исправить баг**: сундук, надпись награды и желтые лучи сияния объединить в единый `this.treasureContainer = this.add.container(cx, cy)` и при победе двигать контейнер целиком (чтобы лучи никогда не улетали в обратную сторону!).
   - Стенки: инициализировать `_shakeBaseX` сразу при создании, добавить линии трещин при падении HP (< 70% и < 35%).
   - Полоски HP: полосы в стиле `drawCasualProgressBar` с белой вспышкой урона.
5. **Бестиарий (`CollectionScene.js`)**:
   - Рамки карточек мобов окрасить в их цвет редкости `mob.rarityColor`.
   - Вверху добавить полоску прогресса открытия коллекции (`Открыто X / 30`).

---

## 4. Acceptance Criteria (Критерии приёмки)
1. `python tools/validate_syntax.py` проходит успешно без синтаксических ошибок.
2. `python tools/run_all_verifications.py` подтверждает отсутствие искажений в 4 вьюпортах.
3. `python tools/capture_screenshot.py tools/render_screenshot.html hidpi_1080p_gameplay.png` показывает гармоничный, сочный казуальный интерфейс.
4. В бою лучи сокровища летят строго вместе с сундуком.

---

## 5. Важные файлы
- `src/scenes/GameScene.js` — квесты, магазин, нижний док, модалки.
- `src/scenes/BattleScene.js` — арена, сундук, стенки, HP.
- `src/scenes/CollectionScene.js` — бестиарий, карточки.
- `src/utils/helpers.js` — библиотека 3D казуальных кнопок и карточек.
- `AGENT_HANDOFF.md` — полная подробная документация архитектуры.

---

## 6. Что НЕЛЬЗЯ ломать (Строгие инварианты)
- **НЕЛЬЗЯ** менять логическую систему координат **960x540**.
- **НЕЛЬЗЯ** использовать координаты `pointer.x / pointer.y` вместо `pointer.worldX / pointer.worldY`.
- **НЕЛЬЗЯ** вызывать `setScrollFactor(0)`.
- **НЕЛЬЗЯ** создавать текст в обход `createHDText`.
- **НЕЛЬЗЯ** нарушать режим **ZERO TOKEN WASTE** (не оставлять запущенных серверов или бесконечных процессов).
